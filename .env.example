# Database Configuration
# WARNING: These are INSECURE defaults for DEVELOPMENT ONLY
# NEVER use root with blank password in production!
# Change these values and create a .env file before deploying

DB_HOST=127.0.0.1
DB_NAME=l33tos
DB_USER=root
DB_PASS=
